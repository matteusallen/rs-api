// Using span as in empty tag format for support for some older browsers
// Also, not collapsed as <span/> for older browsers
const emptyHTMLElement = `<span></span>`;

// Table row with section divider
const dividerRow = `<tr><td style="padding-bottom: 3px"><hr></td></tr>`;

export { emptyHTMLElement, dividerRow };
